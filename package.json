{
  "name": "x-download",
  "version": "1.0.0",
  "description": "Browser extension to download videos from X/Twitter",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --mode content && vite build --mode background && vite build --mode popup",
    "preview": "vite preview"
  },
  "dependencies": {},
  "devDependencies": {
    "fs-extra": "^11.2.0",
    "vite": "^4.5.9"
  }
}